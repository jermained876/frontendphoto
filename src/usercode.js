/*

const token = res.data.access
const refresh = res.data.refresh


axios.get('http://127.0.0.1:8000/api/account/currentuser',{
               headers:{
                'Authorization': `Bearer ${token}`
               }
           })

           const 



 dispatch({
    type:'LOAD_USER',
    item:record
})

 .then(res=>{console.log(res)
            console.log(res.data)
            dispatch({
                type:'LOAD_USER',
                item:record
            })


*/